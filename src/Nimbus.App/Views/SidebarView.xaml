<UserControl
    x:Class="Nimbus.App.Views.SidebarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nimbus.App.Views">
    <UserControl.Resources>
        <SolidColorBrush x:Key="SidebarPanelBrush" Color="#ECEEF3" />
        <SolidColorBrush x:Key="SidebarDividerBrush" Color="#CDCED6" />
        <SolidColorBrush x:Key="SidebarMutedBrush" Color="#6A6A74" />
        <SolidColorBrush x:Key="SidebarTextBrush" Color="#3F4048" />
        <SolidColorBrush x:Key="SidebarItemHoverBrush" Color="#E5EAF8" />
        <SolidColorBrush x:Key="SidebarItemSelectedBrush" Color="#D3E3FF" />

        <Style x:Key="SidebarSectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="{StaticResource SidebarMutedBrush}" />
            <Setter Property="CharacterSpacing" Value="70" />
        </Style>

        <Style x:Key="SidebarListStyle" TargetType="ListView">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="SelectionMode" Value="Single" />
        </Style>

        <Style x:Key="SidebarItemContainerStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="2,1" />
            <Setter Property="CornerRadius" Value="7" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}" />
        </Style>

        <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver" Color="#E5EAF8" />
        <SolidColorBrush x:Key="ListViewItemBackgroundSelected" Color="#D3E3FF" />
        <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPointerOver" Color="#C8DCFF" />
        <SolidColorBrush x:Key="ListViewItemForegroundSelected" Color="#173A75" />
        <SolidColorBrush x:Key="ListViewItemForegroundSelectedPointerOver" Color="#173A75" />
    </UserControl.Resources>

    <Border Margin="0,0,0,0" Padding="8,8,8,4" Background="{StaticResource SidebarPanelBrush}" BorderBrush="{StaticResource SidebarDividerBrush}" BorderThickness="1" CornerRadius="11">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Favorites" Style="{StaticResource SidebarSectionHeaderStyle}" Margin="8,2,8,8" />
        <ListView
            x:Name="SidebarList"
            Grid.Row="1"
            Style="{StaticResource SidebarListStyle}"
            ItemContainerStyle="{StaticResource SidebarItemContainerStyle}"
            ItemsSource="{Binding Locations}"
            SelectionChanged="OnLocationSelectionChanged"
            Margin="0,0,0,12">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="10,5">
                        <TextBlock
                            Text="{Binding IconKey}"
                            FontFamily="Segoe MDL2 Assets"
                            FontSize="13"
                            VerticalAlignment="Center"
                            Foreground="{StaticResource SidebarMutedBrush}" />
                        <TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" FontSize="13" VerticalAlignment="Center" Foreground="{StaticResource SidebarTextBrush}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock Grid.Row="2" Text="Smart Collections" Style="{StaticResource SidebarSectionHeaderStyle}" Margin="8,0,8,8" />
        <ListView
            x:Name="SavedSearchList"
            Grid.Row="3"
            Style="{StaticResource SidebarListStyle}"
            ItemContainerStyle="{StaticResource SidebarItemContainerStyle}"
            ItemsSource="{Binding SavedSearches}"
            SelectionChanged="OnSavedSearchSelectionChanged"
            Margin="0,0,0,12"
            MaxHeight="140">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="10,5">
                        <TextBlock
                            Text="{Binding IconGlyph}"
                            FontFamily="Segoe MDL2 Assets"
                            FontSize="13"
                            VerticalAlignment="Center"
                            Foreground="{StaticResource SidebarMutedBrush}" />
                        <TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" FontSize="13" VerticalAlignment="Center" Foreground="{StaticResource SidebarTextBrush}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock Grid.Row="4" Text="Tags" Style="{StaticResource SidebarSectionHeaderStyle}" Margin="8,0,8,8" />
        <ListView
            x:Name="TagsList"
            Grid.Row="5"
            Style="{StaticResource SidebarListStyle}"
            ItemContainerStyle="{StaticResource SidebarItemContainerStyle}"
            ItemsSource="{Binding Tags}"
            SelectionChanged="OnTagSelectionChanged"
            MaxHeight="140">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="10,5">
                        <TextBlock
                            Text="{Binding IconGlyph}"
                            FontFamily="Segoe MDL2 Assets"
                            FontSize="13"
                            VerticalAlignment="Center"
                            Foreground="{StaticResource SidebarMutedBrush}" />
                        <TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" FontSize="13" VerticalAlignment="Center" Foreground="{StaticResource SidebarTextBrush}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
    </Border>
</UserControl>
