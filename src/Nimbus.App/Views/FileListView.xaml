<UserControl
    x:Class="Nimbus.App.Views.FileListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nimbus.App.Views">
    <UserControl.Resources>
        <SolidColorBrush x:Key="FileListPanelBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="FileListHeaderBrush" Color="#F2F3F6" />
        <SolidColorBrush x:Key="FileListDividerBrush" Color="#D2D2D8" />
        <SolidColorBrush x:Key="FileListMutedBrush" Color="#6A6A72" />

        <Style x:Key="FinderFileListItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Padding" Value="2" />
            <Setter Property="Margin" Value="4,1" />
            <Setter Property="CornerRadius" Value="6" />
        </Style>

        <Style x:Key="ColumnBrowserListStyle" TargetType="ListView">
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="IsItemClickEnabled" Value="False" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
        </Style>

        <ItemsPanelTemplate x:Key="ListItemsPanel">
            <ItemsStackPanel />
        </ItemsPanelTemplate>

        <ItemsPanelTemplate x:Key="GridItemsPanel">
            <ItemsWrapGrid Orientation="Horizontal" />
        </ItemsPanelTemplate>

        <DataTemplate x:Key="ListModeTemplate">
            <StackPanel Orientation="Horizontal" Spacing="12" Margin="12,3">
                <Grid Width="20" Height="20">
                    <TextBlock
                        Text="{Binding IconGlyph}"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="14"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center" />
                    <Image Source="{Binding ThumbnailPath}" Stretch="UniformToFill" />
                </Grid>
                <TextBlock Text="{Binding DisplayName}" Width="248" TextTrimming="CharacterEllipsis" />
                <TextBlock Text="{Binding DateModified}" Width="160" Foreground="{StaticResource FileListMutedBrush}" />
                <TextBlock Text="{Binding SizeBytes}" Width="120" Foreground="{StaticResource FileListMutedBrush}" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="IconModeTemplate">
            <StackPanel Width="128" Height="118" Margin="8" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Height="70" CornerRadius="8" Background="{StaticResource FileListHeaderBrush}">
                    <Grid>
                        <TextBlock
                            Text="{Binding IconGlyph}"
                            FontFamily="Segoe MDL2 Assets"
                            FontSize="24"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Opacity="0.85" />
                        <Image Source="{Binding ThumbnailPath}" Stretch="UniformToFill" />
                    </Grid>
                </Border>
                <TextBlock Text="{Binding DisplayName}" Margin="0,8,0,0" TextAlignment="Center" TextWrapping="WrapWholeWords" MaxLines="2" TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="ColumnBrowserItemTemplate">
            <StackPanel Orientation="Horizontal" Spacing="8" Margin="10,4">
                <Grid Width="16" Height="16">
                    <TextBlock
                        Text="{Binding IconGlyph}"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="12"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center" />
                    <Image Source="{Binding ThumbnailPath}" Stretch="UniformToFill" />
                </Grid>
                <TextBlock Width="180" Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="GalleryModeTemplate">
            <Border Width="210" Height="174" Margin="10" Padding="12" CornerRadius="10" Background="{StaticResource FileListHeaderBrush}" BorderBrush="{StaticResource FileListDividerBrush}" BorderThickness="1">
                <StackPanel Spacing="8">
                    <Border Height="90" CornerRadius="6">
                        <Grid>
                            <TextBlock
                                Text="{Binding IconGlyph}"
                                FontFamily="Segoe MDL2 Assets"
                                FontSize="32"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Opacity="0.85" />
                            <Image Source="{Binding ThumbnailPath}" Stretch="UniformToFill" />
                        </Grid>
                    </Border>
                    <TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding DateModified}" FontSize="11" Foreground="{StaticResource FileListMutedBrush}" />
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Border Background="{StaticResource FileListPanelBrush}" BorderBrush="{StaticResource FileListDividerBrush}" BorderThickness="1" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid x:Name="ListHeaderGrid" ColumnSpacing="12" Padding="16,8,16,6" Background="{StaticResource FileListHeaderBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="120" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Name" FontWeight="SemiBold" Foreground="{StaticResource FileListMutedBrush}" />
                <TextBlock Grid.Column="1" Text="Date Modified" FontWeight="SemiBold" Foreground="{StaticResource FileListMutedBrush}" />
                <TextBlock Grid.Column="2" Text="Size" FontWeight="SemiBold" Foreground="{StaticResource FileListMutedBrush}" />
            </Grid>
            <ListView
                x:Name="FileList"
                Grid.Row="1"
                ItemContainerStyle="{StaticResource FinderFileListItemStyle}"
                BorderThickness="0"
                Background="Transparent"
                ItemsSource="{Binding Items}"
                ItemsPanel="{StaticResource ListItemsPanel}"
                SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                SelectionChanged="OnSelectionChanged"
                DoubleTapped="OnDoubleTapped" />
            <ScrollViewer
                x:Name="ColumnBrowserHost"
                Grid.Row="1"
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollMode="Enabled"
                VerticalScrollMode="Enabled"
                Visibility="Collapsed">
                <StackPanel x:Name="ColumnBrowserPanel" Orientation="Horizontal" Spacing="8" Padding="8" />
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
