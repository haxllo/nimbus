<UserControl
    x:Class="Nimbus.App.Views.FileListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nimbus.App.Views">

    <Grid>
        <ListView
            x:Name="FileList"
            ItemsSource="{Binding Items}"
            SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
            SelectionChanged="OnSelectionChanged"
            DoubleTapped="OnDoubleTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="12" Margin="8,4">
                        <TextBlock Text="{Binding DisplayName}" Width="280" />
                        <TextBlock Text="{Binding DateModified}" Width="160" />
                        <TextBlock Text="{Binding SizeBytes}" Width="120" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
